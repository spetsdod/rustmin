{
  "name": "rustmin",
  "version": "0.9.0",
  "description": "A process monitor to watch the RustAdmin log file and restart the process if the log file is not being updated.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "RustAdmin",
    "Rustmin"
  ],
  "author": "Sean Pendergrast",
  "license": "ISC",
  "dependencies": {
    "chokidar": "^1.6.1"
  }
}
